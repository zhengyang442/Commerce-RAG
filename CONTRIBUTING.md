# 贡献指南

感谢关注 CommerceRAG。当前项目优先接受可验证的小步改进，而不是一次性增加大量新组件。

## 本地检查

```bash
uv sync --python 3.11 --frozen
bash scripts/check_public.sh
```

轻量检查不需要完整 WANDS 数据、索引或外部 LLM。涉及检索质量的修改还必须在本地完成完整数据评测，并在 PR 中说明：

- 数据 commit 与评测 split；
- 修改前后 nDCG@10、RelevantHit@10、ExactHit@10 和 P95；
- 未标注结果的评测语义；
- 质量、延迟、成本和故障降级的权衡。

## 变更要求

1. 不提交 `.env`、密钥、原始 WANDS 数据、索引、模型缓存或运行报告。
2. 不把未标注结果描述为人工判定的 Irrelevant。
3. 新增模型或数据源时记录来源、固定版本、许可证、校验方式和删除方式。
4. 默认测试不得访问真实 LLM 或产生供应商费用。
5. 商品文本和模型输出必须继续作为不可信输入处理。
6. 新功能必须包含与风险相称的测试和文档更新。

## 提交与 PR

- 使用聚焦、可回滚的提交。
- PR 描述应包含变更、动机、用户影响和验证命令。
- 若实验没有改善默认方案，也应如实记录结果，不为了“功能更多”改变默认值。

## 许可证

项目代码许可证尚未选定。在根目录出现明确的 `LICENSE` 前，不接受包含第三方代码复制的大型贡献。
