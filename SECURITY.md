# 安全策略

## 支持范围

当前公开代码对应本地个人项目，不提供商业 SLA。安全修复优先应用到最新主分支。

## 报告漏洞

请不要通过公开 Issue 发布 API Key、访问令牌、个人查询、未公开域名或可直接利用的漏洞细节。请使用 GitHub 仓库的私密漏洞报告功能；如果该功能不可用，只提交不含敏感细节的 Issue 请求维护者建立私密沟通渠道。

## 部署者责任

- 不复用 Claude Code、CC Switch 或其他开发工具密钥。
- 撤销任何曾经出现在聊天、终端输出或第三方配置中的 Key。
- `.env` 仅保存在部署机器并限制文件权限；应用不会自动加载它。
- 公网环境必须启用 `RAG_PUBLIC_PREVIEW=true` 和精确 Host/Origin。
- Uvicorn 不应直接暴露到公网；应由 HTTPS 反向代理或安全隧道提供外层边界。
- 当前内存限流只适用于单进程个人预览，不能替代分布式限流或 WAF。

## 数据与模型边界

商品文本是外部不可信内容，不能作为系统指令。模型只应看到当前 Top-K EvidencePack，输出必须通过结构化契约和引用校验。价格、库存、配送、促销与售后不在 WANDS 数据中，不能据此生成商业承诺。
