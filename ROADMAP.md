# CommerceRAG v0.5 路线图

冻结日期：2026-08-12

v0.5 的目标是把已完成的 v0.4 从本地工程整理为安全、可验证、可公开访问的个人项目。路线图按三个可独立验收的目标阶段推进。

## 阶段一：仓库治理与 GitHub 作品化

状态：已完成（2026-08-12）

- 私有完整历史与干净公开历史分离。
- 公开应用代码、测试、数据版本和冻结评测清单。
- 完成 README、架构、评测、演进、贡献、安全和变更记录。
- 增加不依赖完整数据与模型的 GitHub Actions。
- 完成工作树与全部公开 Git 历史的密钥、内部文件扫描。

完成条件：公开仓库可从零安装并通过轻量 CI；所有公开量化结论可追溯到版本化 benchmark；内部资料和密钥未进入公开历史。

验收证据：

- 私有与公开仓库已整理为平级目录，公开历史从空仓库建立；
- 公开应用代码与私有 `v0.4` 提交 `5a12010` 逐文件一致；
- 本地与 GitHub Actions 均通过 Ruff、109 个测试、benchmark 契约和公开历史扫描；
- README、架构、评测、演进、贡献、安全和变更记录已经进入公开版本；
- 公开候选文件不包含密钥、内部审查文档、原始数据、索引、模型或运行报告。

本阶段不增加新的 RAG 功能、不重做 UI、不部署公网服务。

## 阶段二：公开 Demo 产品化

状态：已完成（2026-08-13）

- 默认访客模式与可展开开发者模式。
- 中文跨语言、多条件排除和安全拒答三个固定演示故事。
- 默认展示简短回答、Top 3 至 5 商品和可核验证据。
- 搜索与 LLM 分开限流，LLM 故障时继续检索。
- 完成桌面、移动端、键盘和公网安全边界验收。

完成条件：新访客无需理解 RAG 术语即可完成中文检索；三个演示故事稳定；v0.4 冻结回归不下降。

验收证据见 [`docs/STAGE2_ACCEPTANCE.md`](docs/STAGE2_ACCEPTANCE.md)：自动测试、公开 benchmark 契约与敏感信息扫描均已通过；真实数据回归和浏览器验收结果单独记录，避免与历史冻结成绩混淆。

本阶段不增加登录、个性化、支付或高可用集群。

## 阶段三：Mac mini 公网发布与个人网站接入

状态：未开始

- 在持续运行的 Mac mini 部署固定版本。
- FastAPI 只监听本机，通过 Cloudflare Tunnel 暴露确切子域名。
- 使用 `launchd` 管理应用和 Tunnel 的启动、重启与日志。
- 配置外置密钥、额度、故障降级和健康检查。
- 将项目案例页加入个人网站，第一版使用外链而非 iframe。

完成条件：Mac mini 重启后自动恢复；Uvicorn 不直接暴露；GitHub、Demo 与个人网站互相可达；密钥不出现在仓库、日志和响应中。

本阶段不承诺多节点高可用或商业 SLA。

## 变更规则

1. 新需求如果不属于当前阶段，先记录，不插入当前执行范围。
2. 指标比较必须标识数据版本、评测清单、策略和运行环境。
3. Reranker 和全量 LLM Rewrite 保持实验路径，除非新的质量与延迟证据支持改变默认值。
4. 每阶段完成后更新状态、验证证据和变更记录，再进入下一阶段。
